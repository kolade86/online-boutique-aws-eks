apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: online-boutique-dev

resources:
  - ../../base

patches:  # NEW
  - path: replica-patch.yaml

commonLabels:
  environment: dev