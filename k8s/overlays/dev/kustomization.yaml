apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: online-boutique-dev

resources:
  - ../../base

patchesStrategicMerge:
  - replica-patch.yaml  # References the separate patch file

labels:
  - pairs:
      environment: dev