apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - all-services.yaml
  - frontend-ingress.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: online-boutique
      app.kubernetes.io/managed-by: kustomize

configMapGenerator:
  - name: redis-config
    literals:
      - REDIS_ADDR=online-boutique-dev-redis.90rvj7.ng.0001.use1.cache.amazonaws.com:6379