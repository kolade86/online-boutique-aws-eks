apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - all-services.yaml
  - frontend-ingress.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: online-boutique
      app.kubernetes.io/managed-by: kustomize

# ConfigMap will be generated dynamically by overlays
# No configMapGenerator here - this will be handled by the workflow overlays