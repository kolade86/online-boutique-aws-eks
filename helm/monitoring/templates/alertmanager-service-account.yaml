apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccounts.alertmanager.name }}
  namespace: {{ .Release.Namespace }}
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.serviceAccounts.alertmanager.roleArn }}
  labels:
    {{- include "online-boutique-monitoring.labels" . | nindent 4 }}